import * as allowedTags from './allowed-tags'
import * as indentation from './indentation'
import * as noBackgroundOnly from './no-background-only'

export default {
  allowedTags,
  indentation,
  noBackgroundOnly,
}
